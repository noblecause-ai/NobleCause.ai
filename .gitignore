.DS_Store
node_modules/
.env
__pycache__/
*.pyc
.venv/
venv/
gremium/.fable-check/
gremium/.probe-raw/

# lokaler Review-Server + verwaiste Plate-Kopien (nicht committen)
serve/
/ratssaal
/vorraum

# prebuild-Artefakt: static/schedule.json = Kopie der Repo-Root-schedule.json
# (site/package.json prebuild: cp ../schedule.json static/schedule.json) —
# generiert, gehört nicht in den Baum.
site/static/schedule.json

# docs: Zeitstempel-Arbeitsbilder (ChatGPT-Exporte, Bild-Exporte, Screenshots).
# Bewusst nach Go-Live behalten, aber nicht im Baum — sie blähen nur den Review-Diff.
# Die Datei selbst bleibt auf der Platte, nur ungetrackt.
docs/Bild *
docs/ChatGPT Image *
docs/screenshot-*

# QA-Durchgang §7: Puppeteer-Skript + Beleg-Screenshots (Dev-Artefakt, nicht in den Baum)
site/qa-durchgang.mjs
site/qa-belege/
