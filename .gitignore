.DS_Store
node_modules/
.env
__pycache__/
*.pyc
.venv/
venv/
gremium/.fable-check/

# lokaler Review-Server + verwaiste Plate-Kopien (nicht committen)
serve/
/ratssaal
/vorraum

# prebuild-Artefakt: static/schedule.json = Kopie der Repo-Root-schedule.json
# (site/package.json prebuild: cp ../schedule.json static/schedule.json) —
# generiert, gehört nicht in den Baum.
site/static/schedule.json
