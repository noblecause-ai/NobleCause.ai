name: Wart — wöchentlicher Research-Lauf

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

concurrency:
  group: wart-research
  cancel-in-progress: false

permissions:
  contents: write
  issues: write
  actions: write

jobs:
  wart:
    name: Scout recherchiert, Wart entscheidet
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r gremium/requirements.txt

      - name: Scout-Recherche und Wart-Entscheid
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          set -o pipefail
          python3 gremium/run_wart.py 2>&1 | tee run.log

      - name: Schema-Tor (Journal-Eintrag vor dem Commit)
        run: |
          set -o pipefail
          python3 gremium/schema_gate.py journal 2>&1 | tee -a run.log

      - name: Commit Journal & schedule
        id: commit
        run: |
          git config user.name "noblecause-wart"
          git config user.email "wart@noblecause.ai"
          git add journal/ schedule.json
          if git diff --staged --quiet; then
            echo "Keine Änderungen zum Committen."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git commit -m "journal: Wart-Eintrag $(date -u +%Y-%m-%d)"
          git push
          echo "changed=true" >> "$GITHUB_OUTPUT"

      # Ein Push mit GITHUB_TOKEN löst absichtlich keinen weiteren push-Workflow
      # aus. Daher den vorhandenen Deploy nach einem echten Rekord-Commit explizit
      # dispatchen; kein zweiter Build-/Deploy-Pfad und kein langlebiger PAT.
      - name: Deploy auslösen
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run deploy.yml --ref master

      - name: Behobenen Wart-Alarm schließen
        env:
          GH_TOKEN: ${{ github.token }}
        run: bash .github/scripts/resolve-ci-failure.sh "ci-failure:wart" "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

      - name: Rohantwort sichern (bei Fehlschlag)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: wart-raw-${{ github.run_id }}
          path: |
            journal/
            run.log
          retention-days: 30
          if-no-files-found: warn

      - name: Fehler als Issue melden
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LABEL="ci-failure:wart"
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          gh label create "$LABEL" --color B60205 --description "Automatischer CI-Fehler" 2>/dev/null || true
          TAIL=$(tail -c 3000 run.log 2>/dev/null || echo "(kein Log erfasst)")
          BODY=$(printf '**Wart-Lauf fehlgeschlagen:** %s\n\n```\n%s\n```' "$RUN_URL" "$TAIL")
          NUM=$(gh issue list --label "$LABEL" --state open --json number --jq '.[0].number // empty')
          if [ -n "$NUM" ]; then
            gh issue comment "$NUM" --body "$BODY"
          else
            gh issue create --title "CI-Fehler: Wart-Lauf" --label "$LABEL" --body "$BODY"
          fi
